<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color_green_accent_700</item>
        <item name="colorPrimaryDark">@color/color_green_600</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Registration steps. -->
    <style name="registration_buttons">
        <item name="android:layout_width">@dimen/btn_steps_size</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_margin">@dimen/com_facebook_share_button_padding_bottom</item>
        <item name="android:layout_centerInParent">true</item>
    </style>

    <style name="family_radio">
        <item name="android:layout_margin">@dimen/activity_vertical_margin</item>
        <item name="android:layout_width">100dp</item>
        <item name="android:layout_height">100dp</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:textSize">@dimen/txt_size_16</item>
    </style>

    <style name="steps_radio">
        <item name="android:layout_margin">@dimen/activity_vertical_margin</item>
        <item name="android:layout_width">60dp</item>
        <item name="android:layout_height">60dp</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:textSize">@dimen/txt_size_16</item>
        <item name="android:button">@null</item>
    </style>

    <!-- Drawer menu. -->
    <style name="drawer_menu_item">
        <item name="android:layout_margin">@dimen/padding_item</item>
        <item name="android:layout_width">20dp</item>
        <item name="android:layout_height">20dp</item>

    </style>

</resources>
